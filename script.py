import sys
import argparse
print("Hello, world!")

parser = argparse.ArgumentParser(description='Procecc some integers.')
parser.add_argument('integers', metavar='N', type=int, nargs='+', help='an integer for the accumulator')

args=
print(sys.argv)